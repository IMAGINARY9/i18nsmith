// Minimal entry for the translation package so TypeScript emits a build artifact
// This file intentionally exports a tiny placeholder to ensure `dist/index.js` and
// `dist/index.d.ts` are generated for consumers.

export const __i18nsmith_translation_placeholder = {} as const
