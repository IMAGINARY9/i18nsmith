{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "declaration": true,
    "declarationMap": true,
    // "composite": true, // Temporarily disabled - old architecture to be replaced by framework adapters
    "noEmit": true // Prevent emitting files while disabled
  },
  "include": ["src/**/*"],
  "references": [
    { "path": "../core" }
  ]
}
